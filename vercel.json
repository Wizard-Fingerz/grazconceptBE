{
    "builds": [
        {
            "src": "globalconceptBE/wsgi.py",
            "use": "@vercel/python",
            "config": {
                "runtime": "python3.9",
                "outputDirectory": "staticfiles"
            }
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "globalconceptBE/wsgi.py"
        }
    ],
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py makemigrations --noinput && python manage.py migrate --noinput"
}
